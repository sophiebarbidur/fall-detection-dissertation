# Data Folder

The UP-Fall dataset is not included in this repository due to size and licensing.

To run the pipeline:
1. Download the UP-Fall dataset from https://sites.google.com/up.edu.mx/har-up/
2. Place `CompleteDataSet.csv` in this folder
3. Run `python ../src/prepare_upfall.py` from the project root to generate windowed arrays